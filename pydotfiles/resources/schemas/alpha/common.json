{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
        "version": {
            "enum": ["alpha"]
        },
        "schema": {
            "enum": ["core", "default_settings", "developer_environments"]
        }
    },
    "required": ["version", "schema"]
}
